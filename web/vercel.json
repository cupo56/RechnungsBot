{
  "buildCommand": "if [ -d ../src ]; then cp -r ../src ./api/src; fi && next build",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" }
  ],
  "functions": {
    "api/index.py": {
      "maxDuration": 60
    }
  }
}
